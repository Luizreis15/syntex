---
description: Shell + Dashboard Visual System v2 congelados — não alterar incidentalmente
alwaysApply: true
---

# Syntex Visual System v2 — Shell e Dashboard congelados

## Pontos de retorno

- **App Shell:** `9aeff69` (`feat(web): consolidate Syntex Visual System v2 app shell`)
- **Dashboard / Command Center:** `b7ca674` (`feat(web): consolidate Syntex command center dashboard`)

## Congelado — não “melhorar” de passagem

É proibido alterar estes artefatos de forma incidental. Mudança exige pedido
explícito e justificativa.

### Shell

- `apps/web/components/layout/sidebar.tsx`
- `apps/web/components/layout/topbar.tsx`
- `apps/web/components/layout/shell.tsx`
- `apps/web/components/layout/nav-config.ts` (exceção: `built: false` → `true` ao ligar módulo)
- `apps/web/components/ui/syntex-command.tsx`
- `apps/web/components/ui/syntex-competence-scope.tsx`
- `apps/web/app/globals.css` (tokens / `surface-command`)
- `apps/web/tailwind.config.ts`
- `apps/web/app/layout.tsx`
- `design/SYNTEX-UI.md` (só com decisão de produto)

### Dashboard / Command Center

- `apps/web/app/(shell)/painel/page.tsx`
- `apps/web/features/dashboard/**`

Command Header, métricas flat, painel de cobranças, rail de ações e empty
states aprovados. Não reinterpretar composição para o próximo módulo.

## Se achar que precisa tocar

**Pare.** Justifique (por quê, o que, impacto) e espere aprovação.

## Permitido sem unfreeze

- Consumir tokens/classes existentes em telas novas
- Bug comprovado que quebra shell/dashboard (mencionar explicitamente)
- Nav: `built: false` → `true` + href + permission, sem redesign visual
