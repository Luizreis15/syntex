# Syntex — Fase operacional (após frontend aprovado)

A fundação de domínio está em `_arquivo_fundacao/`.
A lei visual rígida / freeze histórico está em `_arquivo_design/`.

## Precedência

1. Código atual  
2. ADRs recentes — **ADR-020** governa fase + frontend  
3. `docs/OPERATIONAL-BASELINE.md`

`_arquivo_*` = histórico, não lei.

## Objetivo agora

**Operação real** via vertical slices. Frontend premium está **congelado**
(`d31dcc1` / `docs/FRONTEND-APPROVED-BASELINE.md`).

## Autorizado

- Domínio, API, migrations incrementais, testes, permissões de fatia
- Substituir DEV_DEMO por queries reais **módulo a módulo** (slice explícito)
- Novas UIs de módulo **reutilizando** Visual System existente

## Proibido (sem aprovação PO / ADR)

- Redesign de shell, topbar, sidebar, tokens globais, linguagem visual
- Novos mocks DEV_DEMO silenciosos em capabilities operacionais
- `if (tenant === 'secabc')`
- Quebrar isolamento tenant / RLS / `service_role` no client
- Apagar `_arquivo_*` ou histórico git sem pedido explícito
- Camada de IA operacional nesta fase

## Contrato dos DEMOs já existentes

- Só onde já identificados (Painel / 360s); comentário `DEMO UI — …`
- Seed DEV ≠ mock (seed é REAL persistido)

## Próximo domínio

Representação sindical — ver `docs/OPERATIONAL-BASELINE.md`.
